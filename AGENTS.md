# Project memory

## Notes

- 禁止运行gradle
- # 用户定下的约束规则 1. **禁止私自gradle编译** — 除非用户明确要求，不得运行任何编译/构建命令 2. **禁止私自推送** — 除非用户说 "push"，不得运行 `git push` 3. **一次 push 一次** — 用户说一次 "push"，只推一次，不得在未授权的情况下再次推送 4. **git 操作限制** — 用户不提 push，不得擅自运行任何 git 命令（commit/add 等也包含在内）
